import { PageContainer, Title } from './home.styles';

const Home = () => {
    return (
        <PageContainer>
            <Title>Welcome!</Title>
        </PageContainer>
    );
};

export default Home;